import UIKit

final class {{ module_info.name }}ViewController: UIViewController {
    let presenter: {{ module_info.name }}PresenterProtocol

    init(presenter: {{ module_info.name }}PresenterProtocol) {
        self.presenter = presenter
        super.init(nibName: nil, bundle: nil)
    }

    override func viewDidLoad() {
        super.viewDidLoad()

        presenter.setup()
    }
}

extension {{ module_info.name }}ViewController: {{ module_info.name }}ViewProtocol {}